# Progress Log
Run: 3c7e3b1d-4c81-45b1-b656-c5d76a3013aa
Task: Greenroom MVP: Backstage-native catalog foundation
Started: 2026-03-24 15:18 CET

## Codebase Patterns
- Backstage catalog model and validators live in `lib/catalog-shared.ts`; entity refs normalize to `Component:default/<name>` when kind/namespace are omitted and enforce Backstage kind casing.
- Tests run with Vitest via `npm test`; type safety is checked with `npm run typecheck` or `npm run build`.
- Catalog content is sourced from `content/catalog/**/catalog-info.yaml` and loaded with `loadCatalogEntitiesFromYaml`; relationships/facets/filters are derived in `lib/catalog-core.ts` and consumed via `lib/content.ts` helpers.

---

## 2026-03-24 15:18 CET - US-001: Backstage entity schema and ref validation
- Added Backstage entity envelope validation covering apiVersion/kind/metadata/spec for Component, API, System, Resource, Domain, and Location, including apiVersion allowlist and required spec fields.
- Normalized metadata validation (default namespace, name pattern enforcement, annotations/links/tags checks) and tightened entity ref parsing errors with canonical formatting defaults.
- Added tests for schema validation success/failure cases and entity ref normalization/error handling.
- Files changed: `lib/catalog-shared.ts`, `tests/catalog-schema.test.ts`.
- **Learnings:** Validators enforce Backstage apiVersions (`backstage.io/v1alpha1|v1beta1|v1`) and require spec owners/types where appropriate; metadata names must match `[a-z0-9_.-]` and default namespaces to `default`; entity refs use canonical `Kind:namespace/name` formatting with clear errors on bad segments.
---

## 2026-03-24 16:23 CET - US-007: Seed Backstage catalog content
- Added sample Backstage catalog data in `content/catalog/catalog-info.yaml` for Domain, System, Component, API, Resource, and Location, including relationships (domain->system, system->components/APIs/resources, component provides/consumes APIs and depends on resources).
- Implemented catalog YAML loader (`lib/catalog-loader.ts`) plus core relationship/facet/filter helpers in `lib/catalog-core.ts`, wired through `lib/content.ts` for page consumption, and updated UI components to use Backstage kinds, namespaces, and systems.
- Expanded tests and fixtures covering loader behavior, catalog accessors, relationships, facets/filters, and catalog pages; ensured hero/copy and group ordering align with Backstage kinds.
- Files changed: catalog content under `content/catalog/`, core logic in `lib/catalog-core.ts`/`lib/content.ts`, loader/tests in `lib/catalog-loader.ts` + `tests/catalog-loader.test.ts`, UI components/tests (`components/catalog-*`, `app/catalog/page.tsx`, catalog-related tests), new fixtures under `tests/fixtures/`.
- **Learnings:** Catalog facets now include owners/tags/kinds/namespaces/systems; system/domain references may be namespace-qualified (e.g., `platform/developer-portal`) and must be parsed with kind prefixes before matching; relationships are derived from Backstage semantics rather than custom fields.
---
