# Progress Log
Run: e39b16bd-86d5-4fee-800b-fce005b60469
Task: US-002 Catalog index and facet filters
Started: 2026-03-24 01:43 CET

## Codebase Patterns
- Catalog grouping uses catalogKindOrder; keep all kinds present with empty arrays to maintain ordering.
- Catalog filters apply AND semantics for owner, team, and tags; tag filters are deduped and require all requested tags.
- Shared catalog helpers (types, catalogKindOrder, filterCatalogEntities, groupCatalogEntities) live in lib/catalog-shared so client components stay free of node:fs/path imports.
- Tests run with `npm test` (Vitest).
- Component tests can traverse React element trees directly (using React.isValidElement/Children) to inspect props and invoke handlers without a DOM environment.

---
## 2026-03-24 01:43 CET - US-002-01: Harden catalog content facets and filter helpers
- Added normalized tag handling and tightened filterCatalogEntities for combined owner/team/multi-tag AND semantics.
- Ensured catalog facets remain unique and sorted even when optional owner/team/tag fields are missing on entities.
- Expanded content helper tests for empty facets, combined filters, multi-tag AND, and grouping order after filtering.
- Files changed: lib/content.ts, tests/content.test.ts, progress-e39b16bd-86d5-4fee-800b-fce005b60469.txt
- **Learnings:** groupCatalogEntities relies on catalogKindOrder to preserve predictable ordering and empty arrays; filterCatalogEntities dedupes requested tags and requires all requested tags; getCatalogFacets omits undefined fields while keeping derived facets sorted.
---
## 2026-03-24 01:56 CET - US-002-02: Add catalog filter controls component (owner, team, tag)
- Added a client-side CatalogFilterControls component with simple select inputs for owner, team, and tag, each with an explicit "All" option and accessible labels.
- Normalized filter change handlers to emit selected values and convert cleared selections to undefined for resets.
- Added tests covering facet-derived options rendering and callback behavior for setting and clearing filters.
- Files changed: components/catalog-filter-controls.tsx, tests/catalog-filter-controls.test.tsx, progress-e39b16bd-86d5-4fee-800b-fce005b60469.txt
- **Learnings:** rendering function components directly in tests returns the element tree, allowing traversal to access nested props and invoke onChange handlers without a DOM.
---
## 2026-03-24 02:10 CET - US-002-03: Wire catalog page to apply filters and display grouped results
- Wired the catalog landing page to a client CatalogPageContent that manages owner/team/tag filters and feeds filtered, regrouped entities to CatalogGroups while preserving hero/copy.
- Extracted shared catalog helpers to lib/catalog-shared and added deriveGroupedCatalog so filtering/grouping stays consistent across client usage and tests without pulling node:fs/path into the client bundle.
- Expanded catalog page tests to cover hero/filters/default grouping, empty states, filtered group results (owner/tag), and reset behavior; build/typecheck now pass with the interactive filtering.
- Files changed: app/catalog/page.tsx, components/catalog-page-content.tsx, components/catalog-groups.tsx, components/catalog-filter-controls.tsx, lib/catalog-shared.ts, lib/content.ts, tests/catalog-page.test.tsx
- **Learnings:** keeping shared catalog logic in a server-safe module lets client components reuse it without bundling filesystem deps; deriveGroupedCatalog ensures grouped empty states remain intact after filtering and when filters reset.
---
